<p>Widgets</p>
<div style="position: absolute;">
  {{#each content as |widget|}}
    <div style="{{widget.style}}">
      <h2>{{widget.name}}</h2>
      {{{widget.body}}}
    </div>
  {{/each}}
</div>
